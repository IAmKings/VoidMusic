# 验证记录

日期：2026-09-07  
设备：PJZ110 / Android 16

## 自动化结果

- 回归测试先在旧生产投影入口稳定失败，纯 Kotlin 单应矩阵替换后通过。
- `:app:testDebugUnitTest`：通过，包含生产投影、异常四边形与播放门禁测试。
- `:app:lintDebug`：通过。
- `:app:assembleDebug`：通过。
- `:app:assembleRelease`：通过，专用发布签名与 R8/资源压缩正常。
- PJZ110 Android 仪器测试：27/27 通过，包含 3 条新增 Compose 校准测试。
- `git diff --check`：通过。

## 真机结果

- 校准页显示 4×16 共 64 格，确认和取消按钮可见且可用。
- 单元格按投影相邻中心间距绘制，四行不再重叠成色带。
- 校准无需拖动即可确认；保存后主界面立即显示网格。
- 签名 Release 连续三轮冷启动均恢复 64 格网格。
- 播放后按钮切换为停止，网格继续显示；停止正常。
- Debug 使用 `.debug` 独立包名，与签名正式版并存，避免测试安装器造成签名冲突。

## 用户验收

- 2026-09-07：用户确认最终正式测试包可正常播放，同意归档提交。
- 校准、网格显示、播放与停止闭环验收完成。
